import React from 'react'
import c from "./constants";
const Experience = ({experiences}) => {
  return (
    <div >
    {/* <h4 className='text-[#FCC73F]  text-4xl font-bold p-10 '> WORK EXPERIENCE   </h4>

    <div className=' grid grid-cols-2 p-20 gap-9 '>
      {
        experiences.map((item, index) => (
          <div 
          key={index}>
            <h3 className='text-white'>{item.companyName}</h3>
            <h4 className='text-white'>{item.role}</h4>
            <p className='text-white'>{item.skills} </p>
            <p className='text-white'>{item.responsibility} </p>
            <p className='text-white'>{item.location} </p>
            <p className='text-white'>{item.startDate} </p>
            <p className='text-white'>{item.endDate} </p>

          </div>
        ))
      }
    </div> */}
    </div>

  )
}

export default Experience